spring:
  cloud:
    nacos:
      server-addr: @nacos.address@
      discovery:
        namespace: @nacos.namespace@
        cluster-name: YL
      config:
        namespace: @nacos.namespace@
        file-extension: yaml